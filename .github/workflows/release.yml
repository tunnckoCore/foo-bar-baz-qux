# workflow file from npm docs but using bun
name: release
on:
  push:
    branches:
      - master # or main

permissions:
  contents: read # for checkout

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    permissions:
      contents: write # to be able to publish a GitHub release
      issues: write # to be able to comment on released issues
      pull-requests: write # to be able to comment on released pull requests
      id-token: write # to enable use of OIDC for trusted publishing and npm provenance
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2 # v2
        with:
          bun-version: "latest"
      - name: Install Sigstore CLI
        run: |
          curl -L https://github.com/sigstore/sigstore-go/releases/download/v0.5.0/sigstore-linux-amd64 -o sigstore
          chmod +x sigstore
      - name: Install dependencies
        run: bun install
      # - name: Verify the integrity of provenance attestations and registry signatures for installed dependencies
      #   run: npm audit signatures
      - name: Release
        run: |
          bun run scripts/publish-sigstore.ts
